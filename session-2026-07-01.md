# PicPulse Session — 1 July 2026

## Multi-language Web App
- Replaced entire `app/index.html` with FR/EN/ES/AR version
- Cairo font, flag toggles, RTL for Arabic
- Theme tokens consolidated (brown/gold dark, beige/brown/gold light)
- JSZip for bulk ZIP download of renamed images

## Flare Detection Fixes
- Background exclusion: regions touching 2+ edges skipped as background
- Grid/document suppression: if >5 circular candidates, score set to 0

## OpenRouter AI Vision Mode
- New checkbox "AI Vision for bad labels" + API key field with Save button
- Sends low-confidence (<30%) MobileNet images to `google/gemini-2.5-flash`
- Canvas scaled to 1200px before API call to avoid huge payloads
- Per-image timeout: 70s when vision enabled, 30s otherwise
- Key saved to localStorage, Save button with "Saved" feedback

## Deployment
- `https://ucfzem.github.io/PicPulse/app/` — Web app
- `https://picpulse-landing.vercel.app` — Landing page
